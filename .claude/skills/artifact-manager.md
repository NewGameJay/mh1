---
name: artifact-manager
description: "Store and retrieve query results with checksums using configurable storage backends.
Use when asked to 'save query results', 'store artifact', 'load cached data',
'get artifact by run_id', or 'list re"
user_invocable: true
---

# /artifact-manager

Store and retrieve query results with checksums using configurable storage backends.
Use when asked to 'save query results', 'store artifact', 'load cached data',
'get artifact by run_id', or 'list re

## Instructions

This is an MH1 skill. To execute it:

1. Read the full skill definition at `skills/operations-skills/artifact-manager/SKILL.md`
2. Follow the **Process** section step by step
3. Use available MCP tools (HubSpot, Snowflake, Firebase, Firecrawl) as needed
4. Gather required inputs from the user if not provided
5. Deliver outputs as specified in the skill

## Category
operations

## Triggers
None specified

## Source
`skills/operations-skills/artifact-manager/SKILL.md`
