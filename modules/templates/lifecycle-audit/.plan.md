# Execution Plan: {{MODULE_NAME}}

## Module Info
- **Module ID**: {{MODULE_ID}}
- **Client**: {{CLIENT_ID}}
- **Template**: lifecycle-audit
- **Status**: {{STATUS}}
- **Estimated Duration**: 2-4 hours

---

## Pre-flight Checks

- [ ] CRM credentials configured in MCP
- [ ] Client datasource config exists at `clients/{{CLIENT_ID}}/config/datasources.yaml`
- [ ] Budget approved (estimated $5-15)
- [ ] Input requirements validated

---

## Skills to Execute

```yaml
skills:
  - name: lifecycle-audit
    description: Core lifecycle stage analysis
    required: true
    inputs:
      tenant_id: "{{CLIENT_ID}}"
      limit: 1000
      execution_mode: "suggest"
    checkpoint: true
    expected_outputs:
      - summary (stage distribution, health score)
      - bottlenecks (conversion issues)
      - at_risk (churning accounts)
      - upsell_candidates (expansion opportunities)
      - recommendations (action items)

  - name: cohort-retention-analysis
    description: Build retention curves by cohort
    required: false
    depends_on: lifecycle-audit
    condition: warehouse_access_available
    inputs:
      client_id: "{{CLIENT_ID}}"
      lookback_months: 12
      min_cohort_size: 30
    checkpoint: true
    expected_outputs:
      - retention_curves
      - cohort_comparison
      - retention_drivers
      - churn_analysis

  - name: churn-prediction
    description: Identify at-risk accounts with detailed scoring
    required: false
    depends_on: lifecycle-audit
    inputs:
      client_id: "{{CLIENT_ID}}"
      days_to_churn_warning: 30
    checkpoint: true
    expected_outputs:
      - at_risk_accounts
      - risk_scores
      - churn_signals
```

---

## Execution Checkpoints

| Skill | Status | Started | Completed | Output |
|-------|--------|---------|-----------|--------|
| lifecycle-audit | pending | | | |
| cohort-retention-analysis | pending | | | |
| churn-prediction | pending | | | |

---

## Quality Gates

| Gate | Criteria | Status |
|------|----------|--------|
| Data Validation | Min 20 records, lifecycle stage present | pending |
| Analysis Complete | All required skills succeeded | pending |
| Recommendation Quality | At least 3 actionable recommendations | pending |
| Eval Score | >= 0.8 for auto-delivery | pending |

---

## Output Artifacts

| Artifact | Path | Format |
|----------|------|--------|
| Stage Distribution | `outputs/stage_distribution.json` | JSON |
| At-Risk Accounts | `outputs/at_risk.json` | JSON |
| Recommendations | `outputs/recommendations.md` | Markdown |
| Full Report | `outputs/lifecycle_audit_report.md` | Markdown |
| Raw Data | `outputs/raw_analysis.json` | JSON |

---

## Resume Instructions

If execution is interrupted:

1. Check the last successful checkpoint above
2. Review `runs/` directory for partial outputs
3. Run: `module.continue_from_checkpoint()`
4. The module will resume from the next pending skill

---

## Post-Execution Steps

1. Review generated recommendations
2. Validate at-risk accounts against known context
3. Share report with client stakeholder
4. Create follow-up tasks for implementing recommendations

---

## Notes

<!-- Add execution notes here -->
